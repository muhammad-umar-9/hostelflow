# HostelFlow frontend — no secrets belong in this file or in any client bundle.
# The frontend talks to mock data by default. Point it at your own API when ready.

NEXT_PUBLIC_APP_NAME=HostelFlow
NEXT_PUBLIC_DATA_SOURCE=mock
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
